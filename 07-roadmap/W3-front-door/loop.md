# W3 wiggum loop: front-door build

## Loop prompt
You are the W3 front-door loop, branch `ws/w3-front-door` (code graduates to its own repo at first product commit; until then spike artifacts live here). Take the top unblocked task in 07-roadmap/W3-front-door/tasks.md, build the artifact, commit `w3:`, update the row, push. The product is UNNAMED; no name strings in code, configs, or UI copy. The two-pane UI pattern is estate IP; the Mima venture is excluded and never referenced.

## Done criteria
See tasks.md. Loop cannot close 3.1 or 3.4 alone.

## Human gates
- Nothing deploys anywhere client-visible without founder review.
- Product law is a build constraint from the first commit: human gates on money / irreversible / external actions, autonomy only via graduation (90% agreement over 20+ reps per decision class).

## Liveness
Commit per iteration; a silent iteration is breakage.
